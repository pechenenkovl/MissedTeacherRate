<Window x:Class="MissedTeacherRate.WPF.ComasTrustWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MissedTeacherRate.WPF.Controls"
        Title="COMAS - Trust Values" Height="600" Width="1100"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="400"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- Left side: Trust Grid -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Trust Values" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Ellipse Width="12" Height="12" Fill="#228B22" Margin="0,0,5,0"/>
                <TextBlock Text="Direct Trust" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <Ellipse Width="12" Height="12" Fill="#DC143C" Margin="0,0,5,0"/>
                <TextBlock Text="Indirect Trust" VerticalAlignment="Center"/>
            </StackPanel>

            <DataGrid Grid.Row="2" 
                      ItemsSource="{Binding TrustItems}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="All"
                      AlternatingRowBackground="#F0F0F0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Person 1" Binding="{Binding FirstPerson}" Width="70"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding FirstPersonType}" Width="60"/>
                    <DataGridTextColumn Header="Person 2" Binding="{Binding SecondPerson}" Width="70"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding SecondPersonType}" Width="60"/>
                    <DataGridTextColumn Header="Trust" Binding="{Binding TrustValue}" Width="60"/>
                    <DataGridTextColumn Header="Trust Type" Binding="{Binding TrustType}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Vertical Splitter -->
        <GridSplitter Grid.Column="1" 
                      Width="5" 
                      HorizontalAlignment="Stretch" 
                      VerticalAlignment="Stretch"
                      Background="Gray"/>

        <!-- Right side: Graph Visualization -->
        <Grid Grid.Column="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Trust Graph" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Ellipse Width="12" Height="12" Fill="#4169E1" Margin="0,0,5,0"/>
                <TextBlock Text="Teacher" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <Ellipse Width="12" Height="12" Fill="#FFA500" Margin="0,0,5,0"/>
                <TextBlock Text="Student" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Background="White">
                <controls:TrustGraphControl x:Name="TrustGraph" ClipToBounds="True"/>
            </Border>
        </Grid>
    </Grid>
</Window>
